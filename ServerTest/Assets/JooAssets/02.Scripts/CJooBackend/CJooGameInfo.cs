using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using BackEnd;


public class CJooGameInfo : MonoBehaviour
{

	// Use this for initialization
	void Start()
	{
		/*
		//Test
		Param param = new Param();
		Dictionary<string, string> message = new Dictionary<string, string>()
		{
			{ "asdf", "asdf" },
			{ "qwer", "qwer"}
		};

		param.Add("20181127", message);
		param.Add("1234", false);
		Backend.GameInfo.Insert("message", param);
		JsonUtility.FromJson<Dictionary<string, string>>(param.GetJson());
		foreach(var item in message.Keys)
		{
			Debug.Log(item);
		}
		*/
		
	}
	// Update is called once per frame
	void Update ()
	{
		
	}
}
